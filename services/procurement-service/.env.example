# ── Procurement Service Environment Variables ──────────────
# Copy to .env for local development (outside Docker)

PORT=4002
NODE_ENV=development

# ── Database (individual params — used when DATABASE_URL is NOT set) ──
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=smhussain
DB_NAME=procurement_db

# ── OR use a connection string (takes priority if set) ──
# DATABASE_URL=postgres://postgres:smhussain@localhost:5433/procurement_db

# ── RabbitMQ ──────────────────────────────────────────────
RABBITMQ_URL=amqp://rabbit_user:secret@localhost:5672

# ── Inter-service calls ───────────────────────────────────
CATALOG_SERVICE_URL=http://localhost:4001
