# ┌────────────────────────────────────────────────────────┐
# │  Refinery PO System – Environment Variables            │
# │  Copy this to .env and update the values               │
# │  docker-compose.yml reads variables from .env          │
# └────────────────────────────────────────────────────────┘

# ── PostgreSQL ────────────────────────────────────────────
PG_USER=postgres
PG_PASSWORD=smhussain
PG_EXTERNAL_PORT=5433
# NOTE: External port 5433 avoids conflict with your local PostgreSQL on 5432.
#       Inside Docker, services still connect on port 5432 (internal).
#       In pgAdmin, connect to localhost:5433 to see Docker's postgres.

# ── RabbitMQ ──────────────────────────────────────────────
RABBITMQ_USER=rabbit_user
RABBITMQ_PASS=secret
# Management UI: http://localhost:15672 (login with above credentials)
